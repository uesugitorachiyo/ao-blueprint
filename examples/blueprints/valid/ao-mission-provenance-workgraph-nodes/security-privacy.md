# Security And Privacy

No real Telegram tokens, chat IDs, provider credentials, private keys, `.env`
values, local private paths, releases, deployments, uploads, tags, dependency
updates, auth/policy/config widening, direct-main mutation, concurrent mutation,
hidden instruction mutation, or broad public claims are allowed.

Fixtures must use fake IDs, redacted names, and relative public-safe paths.
