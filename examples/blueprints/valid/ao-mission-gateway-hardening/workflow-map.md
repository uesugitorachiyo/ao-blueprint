# Workflow Map

Blueprint pack -> AO Atlas import -> Atlas workgraph and context packs -> first
safe Foundry import -> Foundry readback/fixture implementation -> Command,
Sentinel, Covenant, Promoter, and Architecture alignment -> final verification.

Atlas must compile the workgraph first. Foundry must import only the first safe
node and must not treat handoff generation as completion.
